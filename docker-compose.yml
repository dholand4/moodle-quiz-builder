services:
  frontend:
    build: .
    restart: unless-stopped
    ports:
      # Na VPS fica em http://IP (porta 80). Para testar local sem conflito:
      #   PORT=8080 docker compose up -d --build  ->  http://localhost:8080
      - "${PORT:-80}:80"
